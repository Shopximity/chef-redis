{
  "run_list": [ "recipe[redis]",
		"recipe[redis::default]",
                "recipe[redis::server_source]"
  ],
  
  "redis" : {
     "config" : {
                 "listen_port" : <port number here>,
                 "requirepass" : "password here"
    }
  }
}
